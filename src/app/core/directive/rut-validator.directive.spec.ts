import { RutValidatorDirective } from './rut-validator.directive';

describe('RutValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RutValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
